// =============================================================================
// Переменные: верхняя полоса (чёрная)
// =============================================================================
$header-top-bar-height: 16px;
$header-top-bar-text: var(--color-text-on-dark);
$header-top-bar-font-size: 12px;
$header-top-bar-gap: 18px;
$header-top-bar-padding-right: 48px;
$header-top-bar-padding-h: 24px;

// =============================================================================
// Переменные: основная полоса (логотип, поиск, навигация)
// =============================================================================
$header-font-family: Roboto, 'Helvetica Neue', sans-serif;
$header-logo-color: var(--color-accent-black);
$header-logo-font: 'Spartan', $header-font-family;
$header-logo-size: 40px;
$header-main-padding-left: 56px;
$header-main-padding-right: 20px;
$header-main-padding-top: 16px;
$header-main-padding-bottom: 22px;
$header-main-bar-gap: 60px;

$header-placeholder: var(--color-placeholder);
$header-search-border: var(--color-border-light);
$header-search-height: 36px;
$header-search-to-nav-gap: 8px;

$header-nav-primary-color: var(--color-accent-black);
$header-nav-primary-size: 13px;
$header-nav-primary-gap: 24px;

$header-nav-user-size: 15px;
$header-nav-user-gap: 1px;
$header-nav-user-width: 370px;

/** Итоговая высота шапки (синхрон с app.component и side-panel) */
$header-total-height: 126px;

// =============================================================================
// Корневой блок шапки
// =============================================================================
.header {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  height: $header-total-height;
  z-index: 100;
  background: var(--color-background-white);
  box-shadow: none;
  font-family: $header-font-family;
  // display: flex;
  // align-items: start;
}

// =============================================================================
// Верхняя чёрная полоса
// =============================================================================
.top-bar {
  background: var(--color-accent-black);
  color: $header-top-bar-text;
  min-height: $header-top-bar-height;

  .top-bar__row {
    display: flex;
    align-items: center;
    min-height: $header-top-bar-height;
    height: $header-top-bar-height;
    width: 100%;
    height: 32px;
    padding: 0 50px 0 24px;
    box-sizing: border-box;
  }
}

.top-bar__spacer {
  flex: 1;
  min-width: 0;
}

.top-bar__links {
  display: flex;
  gap: $header-top-bar-gap;
  align-items: center;
}

.top-bar__link {
  color: var(--color-background-white) !important;
  font-family: 'Regular', 'Courier New', monospace;
}

// =============================================================================
// Основная полоса (Material toolbar)
// =============================================================================

.searchTOP {
  background-color: gray;
}

.main-bar {
  position: relative;
  width: 100%;
  // background: var(--color-background-white) !important;
  background-color: black;
  box-shadow: none;

  .mat-toolbar-row,
  .main-bar__row {
    background: var(--color-background-white);
    max-width: 1280px;
    height: 110px;
    margin: 0 auto;
    padding: $header-main-padding-top $header-main-padding-right $header-main-padding-bottom
      $header-main-padding-left;
    display: grid;
    grid-template-columns: auto 1fr;
    gap: $header-main-bar-gap;
    align-items: start;
    min-height: auto;
    box-sizing: border-box;
  }
}

// Контентная колонка: 100% оставшейся ширины (логотип — отдельная колонка)
.main-bar__content {
  display: flex;
  flex-direction: column;
  width: 100%;
  min-width: 0;
  gap: $header-search-to-nav-gap;
  align-items: stretch;
  background-color: black;
}

// Контейнер блока юзера: верхняя строка, кнопки справа (адаптируется под ширину)
.main-bar__user-wrap {
  display: flex;
  justify-content: flex-end;
  align-items: center;
  flex-shrink: 0;
  min-height: 0;
}

// =============================================================================
// Логотип
// =============================================================================
.logo {
  font-size: $header-logo-size;
  line-height: $header-logo-size;
  font-weight: 400;
  letter-spacing: 0.07em;
  color: $header-logo-color;
  text-decoration: none;
  white-space: nowrap;
  font-family: $header-logo-font;
  margin-left: -125px;
  margin-top: 7px;
  background-color: green;

  &:hover {
    color: $header-logo-color;
    opacity: 0.85;
  }
}

// =============================================================================
// Блок поиска
// =============================================================================
.search-block {
  margin-left: 10px;
  display: flex;
  flex-direction: column;
  align-items: stretch;
  gap: $header-search-to-nav-gap;
  min-width: 0;
  width: 100%;
}

.search-form {
  width: 100%;
  min-width: 0;
  max-width: 100%;
}

.search-field {
  display: flex;
  align-items: flex-end;
  gap: 8px;
  width: 100%;
  min-height: $header-search-height;
  padding-bottom: 4px;
  border-bottom: 1px solid $header-search-border;
  background: var(--color-background-white);
  font-family: $header-font-family;
  transition: border-color 0.2s ease;

  &:focus-within {
    border-bottom-color: var(--color-accent-black);
  }
}

.search-field__icon {
  flex-shrink: 0;
  color: $header-placeholder;
  font-size: 20px;
  width: 20px;
  height: 20px;
  line-height: 1;
}

.search-field__input {
  flex: 1;
  min-width: 0;
  padding: 0;
  border: none;
  background: transparent;
  font-size: 15px;
  line-height: 1.2;
  color: var(--color-accent-black);
  outline: none;
  font-family: inherit;

  &::placeholder {
    color: $header-placeholder;
  }
}

// =============================================================================
// Навигация: Главная / Аукцион / Категории
// =============================================================================
.nav-primary {
  display: flex;
  gap: $header-nav-primary-gap;
  justify-content: center;
}

$header-nav-highlight-bg: var(--color-background-highlight);
$header-nav-text: var(--color-text-primary);

.nav-primary__link {
  color: $header-nav-text !important;
  text-decoration: none;
  font-size: $header-nav-primary-size !important;
  font-weight: 500 !important;
  font-family: $header-font-family;
  text-transform: none !important;
  letter-spacing: normal !important;
  padding: 6px 14px;
  border-radius: 999px;

  &:hover {
    color: $header-nav-text !important;
  }

  &--active {
    color: $header-nav-text !important;
    text-decoration: underline;
    text-underline-offset: 5px;
    text-decoration-thickness: 1px;
  }
}

// =============================================================================
// Навигация: Избранное, Профиль и т.д.
// =============================================================================
.nav-user {
  flex-shrink: 0;
  min-width: 0;

  .nav-user__link.mat-mdc-button,
  button[mat-button].nav-user__link {
    --mat-button-text-label-text-color: white;
    --mat-button-text-state-layer-color: white;
    --mat-button-text-ripple-color: #{rgba(28, 27, 31, 0.12)};
  }
}

.nav-user__group {
  display: flex;
  align-items: center;
  flex-wrap: nowrap;
}

.nav-user__icon {
  display: none !important;
  visibility: hidden;
  width: 0 !important;
  height: 0 !important;
  margin: 0 !important;
  padding: 0 !important;
  overflow: hidden;
  font-size: 0 !important;
}

.nav-user__link {
  color: $header-nav-text !important;
  text-decoration: none;
  text-decoration-color: $header-nav-text;
  font-size: 14px !important;
  font-weight: 200 !important;
  white-space: nowrap;
  font-family: $header-font-family;
  text-transform: none !important;
  letter-spacing: normal !important;
  padding: 6px 14px;
  border-radius: 999px;

  &:hover {
    color: $header-nav-text !important;
    text-decoration: underline;
    text-underline-offset: 5px;
  }

  &--active {
    color: $header-nav-text !important;
    text-decoration: underline;
    text-underline-offset: 5px;
    text-decoration-thickness: 1px;
    text-decoration-color: $header-nav-text;
    background-color: $header-nav-highlight-bg;
  }
}

// =============================================================================
// Адаптив: переменные высоты шапки
// =============================================================================
$header-search-to-nav-gap-adaptive: 24px;
$header-total-height-1220: 82px;
$header-total-height-900: 72px;
$header-total-height-600: 68px;
$header-total-height-480: 64px;

// =============================================================================
// Tablet (≤1220px)
// =============================================================================
@media (max-width: 1220px) {
  .header {
    height: $header-total-height-1220;
  }

  .top-bar__row {
    padding-left: 16px;
    padding-right: 16px;
  }

  .top-bar__links {
    gap: 10px;
    flex-wrap: wrap;
  }

  .top-bar__link {
    font-size: 11px;
  }

  .main-bar .mat-toolbar-row,
  .main-bar .main-bar__row {
    grid-template-columns: auto 1fr;
    grid-template-rows: auto;
    gap: 6px;
    height: auto;
    min-height: 0;
    padding: 6px 16px 6px 16px;
    min-width: 0;
  }

  .logo {
    font-size: 32px;
    line-height: 32px;
    margin-left: 0;
  }

  .nav-primary {
    gap: 1px;
  }

  .main-bar__user-wrap {
    justify-content: flex-end;
  }

  .nav-user {
    max-width: 300px;
  }

  .nav-user__group {
    flex-wrap: nowrap;
    gap: 2px;
    justify-content: flex-end;
  }

  .nav-user__link {
    font-size: 12px !important;
    padding-left: 8px !important;
    padding-right: 8px !important;
  }

  .search-block {
    gap: 4px;
  }

  .search-field {
    min-height: 28px;
    padding-bottom: 2px;
  }

  .nav-primary__link {
    font-size: 12px !important;
  }
}

// =============================================================================
// Планшет узкий (≤900px)
// =============================================================================
@media (max-width: 900px) {
  .header {
    height: $header-total-height-900;
  }

  .top-bar__row {
    padding-left: 16px;
    padding-right: 16px;
  }

  .top-bar__links {
    gap: 10px;
    flex-wrap: wrap;
  }

  .top-bar__link {
    font-size: clamp(9px, 1.4vw, 11px);
  }

  .main-bar .mat-toolbar-row,
  .main-bar .main-bar__row {
    grid-template-columns: auto minmax(0, 1fr);
    grid-template-rows: auto;
    gap: 10px;
    align-items: center;
    height: auto;
    min-height: 0;
    padding: 8px 16px;
    min-width: 0;
  }

  .logo {
    font-size: clamp(20px, 4vw, 26px);
    line-height: 1.2;
    margin-left: 0;
  }

  .main-bar__content {
    flex-direction: row;
    flex-wrap: nowrap;
    align-items: center;
    gap: 10px;
    min-width: 0;
  }

  .main-bar__user-wrap {
    flex: 0 0 auto;
    flex-shrink: 0;
    order: 2;
  }

  .search-block {
    flex: 1 1 auto;
    width: auto;
    min-width: 0;
    margin-left: 0;
    padding-left: 0;
    gap: 0;
    order: 1;
  }

  .search-form {
    width: 100%;
    min-width: 0;
  }

  .search-field {
    width: 100%;
    min-width: 0;
    min-height: 26px;
    padding-bottom: 2px;
    gap: 6px;
  }

  .search-field__icon {
    font-size: 18px;
    width: 18px;
    height: 18px;
  }

  .search-field__input {
    flex: 1;
    min-width: 0;
    width: 100%;
    font-size: clamp(11px, 1.8vw, 13px);
    box-sizing: border-box;
  }

  .nav-primary {
    gap: 4px;
  }

  .nav-primary__link {
    font-size: clamp(10px, 1.6vw, 12px) !important;
    padding: 4px 10px;
  }

  .nav-user {
    max-width: min(220px, 32vw);
    min-width: 0;
  }

  .nav-user__group {
    flex-wrap: wrap;
    gap: 4px 6px;
    justify-content: flex-end;
    align-content: center;
    max-width: 100%;
  }

  .nav-user__link {
    font-size: clamp(10px, 1.6vw, 11px) !important;
  }
}

// =============================================================================
// Большой мобильный (≤600px)
// =============================================================================
@media (max-width: 600px) {
  .header {
    height: $header-total-height-600;
  }

  .top-bar__row {
    padding-left: 12px;
    padding-right: 12px;
  }

  .top-bar__links {
    gap: 6px;
    flex-wrap: wrap;
  }

  .top-bar__link {
    font-size: clamp(8px, 1.2vw, 10px);
  }

  .main-bar .mat-toolbar-row,
  .main-bar .main-bar__row {
    gap: 6px;
    padding: 6px 12px;
    min-width: 0;
    grid-template-columns: auto minmax(0, 1fr);
  }

  .logo {
    font-size: clamp(16px, 3vw, 22px);
    line-height: 1.2;
    margin-left: 0;
  }

  .search-block {
    padding-left: 6px;
    width: 100%;
    max-width: 100%;
    min-width: 0;
    margin-left: 0;
    gap: 0;
  }

  .search-form {
    width: 100%;
    min-width: 0;
  }

  .search-field {
    width: 100%;
    min-width: 0;
    min-height: 22px;
    padding-bottom: 1px;
    gap: 4px;
  }

  .search-field__input {
    flex: 1;
    min-width: 0;
    width: 100%;
    font-size: clamp(10px, 1.5vw, 12px);
    box-sizing: border-box;
  }

  .search-field__icon {
    font-size: 15px;
    width: 15px;
    height: 15px;
  }

  .nav-primary {
    gap: 2px 5px;
    flex-wrap: wrap;
    justify-content: center;
  }

  .nav-primary__link {
    font-size: clamp(8px, 1.3vw, 10px) !important;
    padding: 2px 5px;
  }

  .nav-user {
    max-width: none;
    min-width: 0;
    flex-shrink: 0;
  }

  .nav-user__group {
    gap: 4px;
    flex-wrap: nowrap;
    justify-content: flex-end;
    align-content: center;
  }
}

// =============================================================================
// Мобильный: кнопки-иконки вместо текста (≤599px)
// =============================================================================
@media (max-width: 599px) {
  .search-block {
    padding-left: 6px;
    width: 90%;
    max-width: 100%;
    min-width: 0;
    margin-left: 0;
    gap: 2px;
  }

  .search-form {
    width: 100%;
    min-width: 0;
  }

  .search-field {
    width: 100%;
    min-width: 0;
    min-height: 22px;
    padding-bottom: 1px;
    gap: 4px;
  }

  .search-field__input {
    flex: 1;
    min-width: 0;
    width: 100%;
    font-size: clamp(10px, 1.5vw, 12px);
    box-sizing: border-box;
  }

  .search-field__icon {
    font-size: 15px;
    width: 15px;
    height: 15px;
  }

  .nav-user__icon {
    display: block !important;
    visibility: visible;
    font-size: 12px !important;
    width: 12px !important;
    height: 12px !important;
    margin: 0;
    padding: 0;
    overflow: visible;
    color: var(--color-background-white) !important;
    flex-shrink: 0;
  }

  .nav-user__label {
    display: none !important;
    visibility: hidden;
    width: 0 !important;
    height: 0 !important;
    overflow: hidden;
    position: absolute;
    clip: rect(0, 0, 0, 0);
  }

  .nav-user__link {
    min-width: 24px;
    width: 24px;
    height: 24px;
    padding: 0 !important;
    border-radius: 6px;
    background-color: var(--color-accent-black) !important;
    color: var(--color-background-white) !important;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    text-decoration: none !important;

    &:hover {
      background-color: var(--color-accent-black-hover) !important;
      color: var(--color-background-white) !important;
      text-decoration: none !important;
    }

    &--active {
      background-color: var(--color-accent-black-active) !important;
      color: var(--color-background-white) !important;
      box-shadow: 0 0 0 2px rgba(255, 255, 255, 0.3);
    }
  }

  .nav-user .nav-user__link.mat-mdc-button,
  .nav-user button[mat-button].nav-user__link {
    min-width: 24px !important;
    width: 24px !important;
    height: 24px !important;
    padding: 0 !important;
  }

  .nav-user__link ::ng-deep .mdc-button__label {
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0;
  }
}

// =============================================================================
// Маленький мобильный (≤480px)
// =============================================================================
@media (max-width: 480px) {
  .header {
    height: $header-total-height-480;
  }

  .top-bar__row {
    padding-left: 10px;
    padding-right: 10px;
  }

  .top-bar__link {
    font-size: clamp(8px, 1.2vw, 10px);
  }

  .main-bar .mat-toolbar-row,
  .main-bar .main-bar__row {
    padding: 6px 10px;
    gap: 6px;
    min-width: 0;
    grid-template-columns: auto minmax(0, 1fr);
  }

  .logo {
    font-size: clamp(14px, 2.8vw, 18px);
    line-height: 1.2;
    margin-left: 0;
  }

  .search-block {
    gap: 0;
    padding-left: 4px;
  }

  .search-field {
    min-height: 20px;
    padding-bottom: 1px;
    gap: 3px;
  }

  .search-field__input {
    font-size: clamp(9px, 1.4vw, 11px);
  }

  .search-field__icon {
    font-size: 13px;
    width: 13px;
    height: 13px;
  }

  .nav-primary {
    gap: 2px 4px;
    flex-wrap: wrap;
    justify-content: center;
  }

  .nav-primary__link {
    font-size: clamp(7px, 1.2vw, 9px) !important;
    padding: 2px 4px;
  }

  .nav-user__group {
    gap: 3px;
  }

  .nav-user__icon {
    font-size: 11px !important;
    width: 11px !important;
    height: 11px !important;
  }

  .nav-user__link {
    min-width: 22px !important;
    width: 22px !important;
    height: 22px !important;
  }

  .nav-user .nav-user__link.mat-mdc-button,
  .nav-user button[mat-button].nav-user__link {
    min-width: 22px !important;
    width: 22px !important;
    height: 22px !important;
  }
}

// =============================================================================
// Очень узкий экран (≤400px)
// =============================================================================
@media (max-width: 400px) {
  .nav-primary__link {
    font-size: clamp(6px, 1.1vw, 8px) !important;
    padding: 1px 3px;
  }

  .nav-user__group {
    gap: 2px;
  }

  .nav-user__icon {
    font-size: 10px !important;
    width: 10px !important;
    height: 10px !important;
  }

  .nav-user__link {
    min-width: 20px !important;
    width: 20px !important;
    height: 20px !important;
  }

  .nav-user .nav-user__link.mat-mdc-button,
  .nav-user button[mat-button].nav-user__link {
    min-width: 20px !important;
    width: 20px !important;
    height: 20px !important;
  }
}
